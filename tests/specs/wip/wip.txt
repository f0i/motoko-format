== (skip) should format ==
module {
  public func e(m : List<Bool>) : List<Bool> {
    let input = List.append();
  }
}
[expect]
module {
  public func e(m : List<Bool>) : List<Bool> {
    let input = List.append();
  };
};
